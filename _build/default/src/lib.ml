(** Modules from /model *)

module Country = Country

module Continent = Continent

module Card = Card

module Player = Player

module World = World

(** Modules from /controller *)

module Game = Game

module GainPhase = Gain_phase

module Attack = Attack

(** Modules from /data *)
module CountryData = Countrydata

module ContinentData = Continentdata

module MovePhase = Move_phase

module CardData = Carddata

module Testdata=Testdata

(** Misc modules *)

module Authors = Authors